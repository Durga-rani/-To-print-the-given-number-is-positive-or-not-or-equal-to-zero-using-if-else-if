// given number is positive or not or equal to zero using if-else-if
#include<stdio.h>
int main()
{
  int n;
  printf("enter a number:");
  scanf("%d",&n);
    
    if(n>0)
  {
      printf("positive");
  }
   else if(n<0)
  {
      printf("negative");
  }
   else
 {
      printf("the number is zero");
  }
 }

Output:
enter a number:1840
positive

enter a number:-2345
negative

enter a number:0
the number is equal to zero

